namespace OctoMap.Runtime
{
    /// <summary>
    /// Provides cached multi-source type arrays for closed generic mapper calls.
    /// </summary>
    internal static class MultiSourceTypeCache<TSource1, TSource2>
    {
        public static readonly Type[] Types = { typeof(TSource1), typeof(TSource2) };
    }

    /// <summary>
    /// Provides cached multi-source type arrays for closed generic mapper calls.
    /// </summary>
    internal static class MultiSourceTypeCache<TSource1, TSource2, TSource3>
    {
        public static readonly Type[] Types = { typeof(TSource1), typeof(TSource2), typeof(TSource3) };
    }

    /// <summary>
    /// Provides cached multi-source type arrays for closed generic mapper calls.
    /// </summary>
    internal static class MultiSourceTypeCache<TSource1, TSource2, TSource3, TSource4>
    {
        public static readonly Type[] Types = { typeof(TSource1), typeof(TSource2), typeof(TSource3), typeof(TSource4) };
    }

    /// <summary>
    /// Provides cached multi-source type arrays for closed generic mapper calls.
    /// </summary>
    internal static class MultiSourceTypeCache<TSource1, TSource2, TSource3, TSource4, TSource5>
    {
        public static readonly Type[] Types = { typeof(TSource1), typeof(TSource2), typeof(TSource3), typeof(TSource4), typeof(TSource5) };
    }

    /// <summary>
    /// Provides cached multi-source type arrays for closed generic mapper calls.
    /// </summary>
    internal static class MultiSourceTypeCache<TSource1, TSource2, TSource3, TSource4, TSource5, TSource6>
    {
        public static readonly Type[] Types = { typeof(TSource1), typeof(TSource2), typeof(TSource3), typeof(TSource4), typeof(TSource5), typeof(TSource6) };
    }

    /// <summary>
    /// Provides cached multi-source type arrays for closed generic mapper calls.
    /// </summary>
    internal static class MultiSourceTypeCache<TSource1, TSource2, TSource3, TSource4, TSource5, TSource6, TSource7>
    {
        public static readonly Type[] Types = { typeof(TSource1), typeof(TSource2), typeof(TSource3), typeof(TSource4), typeof(TSource5), typeof(TSource6), typeof(TSource7) };
    }

    /// <summary>
    /// Provides cached multi-source type arrays for closed generic mapper calls.
    /// </summary>
    internal static class MultiSourceTypeCache<TSource1, TSource2, TSource3, TSource4, TSource5, TSource6, TSource7, TSource8>
    {
        public static readonly Type[] Types = { typeof(TSource1), typeof(TSource2), typeof(TSource3), typeof(TSource4), typeof(TSource5), typeof(TSource6), typeof(TSource7), typeof(TSource8) };
    }

    /// <summary>
    /// Provides cached multi-source type arrays for closed generic mapper calls.
    /// </summary>
    internal static class MultiSourceTypeCache<TSource1, TSource2, TSource3, TSource4, TSource5, TSource6, TSource7, TSource8, TSource9>
    {
        public static readonly Type[] Types = { typeof(TSource1), typeof(TSource2), typeof(TSource3), typeof(TSource4), typeof(TSource5), typeof(TSource6), typeof(TSource7), typeof(TSource8), typeof(TSource9) };
    }

    /// <summary>
    /// Provides cached multi-source type arrays for closed generic mapper calls.
    /// </summary>
    internal static class MultiSourceTypeCache<TSource1, TSource2, TSource3, TSource4, TSource5, TSource6, TSource7, TSource8, TSource9, TSource10>
    {
        public static readonly Type[] Types = { typeof(TSource1), typeof(TSource2), typeof(TSource3), typeof(TSource4), typeof(TSource5), typeof(TSource6), typeof(TSource7), typeof(TSource8), typeof(TSource9), typeof(TSource10) };
    }
}
